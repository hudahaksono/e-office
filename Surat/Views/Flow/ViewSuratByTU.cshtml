@model Surat.Models.Entities.Surat

@{
    var lstKeteranganSurat = new List<SelectListItem>();
    lstKeteranganSurat.Add(new SelectListItem { Text = "", Value = "" });
    lstKeteranganSurat.Add(new SelectListItem { Text = "Terbuka", Value = "Terbuka" });
    lstKeteranganSurat.Add(new SelectListItem { Text = "Tertutup", Value = "Tertutup" });
}

<style>
    #tampilanDokumen {
        height: 70vh;
    }

    @@media (max-height:750px) {
        .attachment {
            margin-top: 145px
        }
    }
</style>

<script src='@Url.Content("~/resources/js/jqueryautocomplete/jquery.autocomplete.js")' type="text/javascript"></script>
<script src='@Url.Content("~/resources/js/pdfobject.min.js")'></script>

<script type="text/javascript">
    $(function () {
        $.validator.unobtrusive.parse(this);
    });
</script>

<div class="row">
    <div class="col-md-12 col-xs-12">
        <div class="x_panel">
            <div class="x_content">
                <div class="row">

                    <div class="demo col-sm-12 col-xs-12 mail_view">
                        <div class="inbox-body">
                            <div class="col-md-12 col-xs-12" style="text-align:center; background-color:beige; font-family: 'Varela Round', sans-serif; margin-bottom:10px;">
                                <h4>PERSETUJUAN SURAT</h4>
                            </div>

                            <div class="mail_heading row">
                                <div class="col-md-12 col-xs-12">
                                    <div class="compose-btn">
                                        <a id="LinkArsip" class="btn btn-sm btn-primary" href="#" onclick="ArsipSurat();"><i class="fa fa-check"></i>&nbsp;&nbsp;Selesai / Arsip</a>
                                        <button id="BtnTolak" class="btn btn-sm btn-danger" type="button"><i class="fa fa-mail-reply-all"></i>&nbsp;&nbsp;Tolak</button>
                                        @*<button id="BtnKembalikan" class="btn btn-sm btn-warning" type="button"><i class="fa fa-mail-reply"></i>&nbsp;&nbsp;Kembalikan</button>*@
                                        <button id="BtnProses" class="btn btn-sm btn-success" type="button"><i class="fa fa-flash"></i>&nbsp;&nbsp;Proses/Setuju</button>
                                    </div>
                                </div>
                                <div class="col-md-12 col-xs-12">
                                    <h4> @Model.Perihal - <small style="color:#1abb9c; font-size:11px;">@Model.ArahSurat</small> </h4>
                                </div>
                            </div>

                            <form id="frmEditSurat" class="form-horizontal form-label-left" style="padding-top:10px;">
                                @Html.HiddenFor(m => m.SuratId, new { @id = "SuratId" })
                                @Html.HiddenFor(m => m.SuratInboxId, new { @id = "SuratInboxId" })
                                @Html.HiddenFor(m => m.TanggalSurat, new { @id = "TanggalSurat" })
                                @Html.HiddenFor(m => m.NomorAgenda, new { @id = "NomorAgenda" })
                                @Html.HiddenFor(m => m.NamaPenerima, new { @id = "NamaPenerima" })
                                @Html.HiddenFor(m => m.Kategori, new { @id = "Kategori" })
                                @Html.HiddenFor(m => m.KeteranganSurat, new { @id = "KeteranganSurat" })
                                @Html.HiddenFor(m => m.Sumber_Keterangan, new { @id = "Sumber_Keterangan" })
                                <div class="row">
                                    <div class="" id="detailSurat">
                                        <div class="row">
                                            <div class="col-md-6 col-xs-12">
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Metode Pengiriman :</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextBoxFor(model => model.Sumber_Keterangan, new { @class = "form-control", @id = "Sumber_Keterangan", @readonly = "readonly", @style = "background-color:white; color:#000000; border-color:#ffffff; box-shadow: none !important; font-family: 'Varela Round', sans-serif; font-size: small;" })
                                                    </div>
                                                </div>

                                                <div class="form-group" id="DivTujuanSurat">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Tujuan Surat:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextBoxFor(model => model.PenerimaSurat, new { @class = "form-control", @id = "penerimasurat", @readonly = "readonly", @style = "background-color:white; color:#000000; border-color:#ffffff; box-shadow: none !important; font-family: 'Varela Round', sans-serif; font-size: small;" })
                                                    </div>
                                                </div>
                                                <div class="form-group" id="DivAsalSurat">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Asal Surat:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextBoxFor(model => model.PengirimSurat, new { @class = "form-control", @id = "pengirimsurat", @required = "required" })
                                                        @Html.ValidationMessageFor(model => model.PengirimSurat, "Asal Surat wajib diisi.")
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Sifat Surat: <span style="color:red">*</span></label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.DropDownListFor(model => model.SifatSurat, new SelectList(@Model.ListSifatSurat, "NamaSifatSurat", "NamaSifatSurat"), "", new { @class = "select2_single form-control input-md", @style = "width:100%", @id = "cmbSifatSurat", @required = "required" })
                                                        @Html.ValidationMessageFor(model => model.SifatSurat, "Sifat Surat wajib dipilih.")
                                                        @*@Html.TextBoxFor(model => model.SifatSurat, new { @class = "form-control", @id = "sifatsurat", @readonly = "readonly", @style = "background-color:white; color:#000000; border-color:#ffffff; box-shadow: none !important; font-family: 'Varela Round', sans-serif; font-size: small;" })*@
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Jenis Surat:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.DropDownListFor(model => model.TipeSurat, new SelectList(@Model.ListTipeSurat, "NamaTipeSurat", "NamaTipeSurat"), "", new { @class = "select2_single form-control input-md", @style = "width:100%", @id = "cmbTipeSurat", @required = "required", @onchange = "JenisSuratChanged()" })
                                                        @Html.ValidationMessageFor(model => model.TipeSurat, "Jenis Surat wajib dipilih.")
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Tanggal Surat:</label>
                                                    <div class="col-md-5 col-sm-5 col-xs-12">
                                                        <div class='input-group date' id='tglSurat' style="margin-bottom: 0px;">
                                                            @Html.TextBoxFor(model => model.TanggalSurat, new { @class = "form-control", @id = "tanggalsurat", @required = "required" })
                                                            <span class="input-group-addon">
                                                                <span class="glyphicon glyphicon-calendar"></span>
                                                            </span>
                                                        </div>
                                                        @Html.ValidationMessageFor(model => model.TanggalSurat, "Tanggal Surat wajib diisi")
                                                    </div>
                                                </div>
                                                <div class="form-group" id="DivTanggalTerima">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Tanggal Terima:</label>
                                                    <div class="col-md-5 col-sm-5 col-xs-12">
                                                        <div class='input-group date' id='tglTerima' style="margin-bottom: 0px;">
                                                            @Html.TextBoxFor(model => model.TanggalTerima, new { @class = "form-control", @id = "tanggalterima", @required = "required" })
                                                            <span class="input-group-addon">
                                                                <span class="glyphicon glyphicon-calendar"></span>
                                                            </span>
                                                        </div>
                                                        @Html.ValidationMessageFor(model => model.TanggalTerima, "Tanggal Terima wajib diisi")
                                                    </div>
                                                </div>
                                                <div class="form-group" id="DivTanggalUndangan">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Tanggal Undangan:</label>
                                                    <div class="col-md-5 col-sm-5 col-xs-12">
                                                        <div class='input-group date' id='tglUndangan' style="margin-bottom: 0px;">
                                                            @Html.TextBoxFor(model => model.TanggalUndangan, new { @class = "form-control", @id = "tanggalundangan" })
                                                            <span class="input-group-addon">
                                                                <span class="glyphicon glyphicon-calendar"></span>
                                                            </span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-md-6 col-xs-12">
                                                <div class="form-group" id="DivNomorAgenda">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Nomor Agenda:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextBoxFor(model => model.NomorAgendaSurat, new { @class = "form-control", @id = "nomoragendasurat", @readonly = "readonly", @style = "background-color:white; color:#000000; border-color:#ffffff; box-shadow: none !important; font-family: 'Varela Round', sans-serif; font-size: small;" })
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Nomor Surat:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextBoxFor(model => model.NomorSurat, new { @class = "form-control", @id = "nomorsurat" })
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Perihal: <span style="color:red">*</span></label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextAreaFor(model => model.Perihal, new { @class = "form-control", @id = "perihal", @rows = 2, @required = "required", spellcheck = "false", autocomplete = "off", autocorrect = "off" })
                                                        @Html.ValidationMessageFor(model => model.Perihal, "Perihal wajib diisi.")
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Keterangan Surat:</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.DropDownListFor(model => model.KeteranganSurat, lstKeteranganSurat, new { @class = "select2_single form-control input-md", @style = "width:100%", @id = "cmbKeteranganSurat", @required = "required" })
                                                        @Html.ValidationMessageFor(model => model.KeteranganSurat, "Keterangan Surat wajib dipilih.")
                                                    </div>
                                                </div>
                                                <div class="form-group">
                                                    <label class="control-label col-md-4 col-sm-4 col-xs-12" style="text-align: left;">Resume Surat</label>
                                                    <div class="col-md-8 col-sm-8 col-xs-12">
                                                        @Html.TextAreaFor(model => model.IsiSingkatSurat, new { @class = "form-control", @id = "isisingkatsurat", @rows = 2, spellcheck = "false", autocomplete = "off", autocorrect = "off" })
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="form-group">
                                            <div class="col-md-12 col-sm-12 col-xs-12">
                                                <button id="btnsimpansurat" type="submit" class="btn btn-success">Simpan</button>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="" id="detailLampiran" style="display:none">
                                        <div id="tampilanDokumen"></div>
                                    </div>
                                </div>
                            </form>

                            <div class="ln_solid"></div>

                            <div class="attachment">
                                <p>
                                    <span><i class="fa fa-paperclip"></i> <label id="LabelJumlahLampiran"></label> file </span>
                                    <span class="btn btn-primary btn-file" id="TombolTambahFile"><i class="fa fa-plus"></i>&nbsp;&nbsp;Tambah<input type="file" name="UploadFromFile" id="txtUploadFile" class="btn btn-primary" accept=".pdf,.jpg,.jpeg,.png,.doc,.docx,.xls,.xlsx" /></span>
                                </p>
                                <ul id="dataplaceholder"></ul>
                            </div>


                            <div class="col-md-10 col-sm-10 col-xs-12" style="padding-top:10px;">
                                <div class="x_panel">
                                    <div class="x_title">
                                        <h2 class="mytitleform">PERJALANAN SURAT</h2>
                                        <div class="clearfix"></div>
                                    </div>
                                    <div class="x_content">
                                        <ul class="list-unstyled timeline" id="inboxplaceholder"></ul>
                                    </div>
                                </div>
                            </div>


                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>


@*<div id='myModalDocViewer' class='modal'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div id='myModalContent'></div>
            </div>
        </div>
    </div>*@


<script type="text/javascript">
    var objpdf = null;
    var redaksi = "";
    $('#tampilanDokumen').on('shown.bs.modal', function () {
        $(this).find('.modal-dialog').css({
            width: 'auto',
            height: '95 %',
            'max-height': '100%'
        });
    });


    $(document).ready(function () {

        $('.select2_single').select2({ width: 'resolve' });

        $('#tglSurat, #tanggalsurat').datetimepicker({
            format: 'DD/MM/YYYY',
            locale: 'id',
            maxDate: moment()
        });
        $('#tglTerima, #tanggalterima').datetimepicker({
            format: 'DD/MM/YYYY HH:mm',
            locale: 'id',
            maxDate: moment()
        });
        $('#tglUndangan, #tanggalundangan').datetimepicker({
            format: 'DD/MM/YYYY HH:mm',
            locale: 'id'
        });

        $("#DivAsalSurat").hide();
        //$("#DivTujuanSurat").hide();
        $("#DivNomorAgenda").hide();
        $("#DivTanggalTerima").hide();
        var arahsurat = '@Model.Arah';
        if (arahsurat == 'Masuk') {
            $("#DivAsalSurat").show();
            $("#DivNomorAgenda").show();
            $("#DivTanggalTerima").show();
        }
        else if (arahsurat == 'Keluar') {
            //$("#DivTujuanSurat").show();
        }

        $("#DivTanggalUndangan").hide();
        var tipesurat = '@Model.TipeSurat';
        if (tipesurat == 'Surat Undangan') {
            $("#DivTanggalUndangan").show();
        }

        @*var redaksi = '@Model.Redaksi';
        if (redaksi !== null && redaksi !== '') {
            if (redaksi == 'Tembusan') {
                $("#LinkArsip").hide();
            }
            //else {
            //    $("#LinkSelesaiSuratInbox").hide();
            //}
        }*@

        loadLampiranSurat();

        // Tulis jumlah lampiran
        var suratid = $('#SuratId').val();
        $.ajax({
            url: '@Url.Action("GetJumlahLampiranSurat", "Flow")?suratid=' + suratid + '',
            success: function (response) {
                $('#LabelJumlahLampiran').html(response);
            },
            error: function (response) { }
        });


        loadSuratHistory();

        // AUTO COMPLETE ASAL SURAT -------------------------------

        $.ajax({
            url: '@Url.Action("GetAsalSurat", "Admin")',
            type: "GET",
            dataType: "JSON",
            success: function (data) {
                $('#pengirimsurat').autocomplete({
                    lookup: data,
                    lookupFilter: function (suggestion, originalQuery, queryLowerCase) {
                        var re = new RegExp('\\b' + $.Autocomplete.utils.escapeRegExChars(queryLowerCase), 'gi');
                        return re.test(suggestion.value);
                    }
                });
            }
        });
        redaksi = '@Model.Redaksi';

        // EOF AUTO COMPLETE ASAL SURAT ---------------------------

        $.unblockUI();
    });

    function loadLampiranSurat() {
        $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
        var id = $('#SuratId').val();
        $.ajax({
            type: 'POST',
            url: '@Url.Action("DaftarLampiranSurat", "Flow")',
            data: { suratid: id },
            success: function (data, textStatus, XMLHttpRequest) {

                if (data == 'noresults') {
                    $('#dataplaceholder').html('');
                }
                else {
                    $('#dataplaceholder').html(data);
                    //let frist = $(data).find('div:first-child a:first-child')[0]
                    //$(frist).trigger('click')
                }
                $.unblockUI();
            },
            error: function (XMLHttpRequest, textStatus, errorThrown) { $.unblockUI(); }
        });
    };


    let isexpand = false
    var BukaFileLampiran = function (id, namafile, kantorid) {
        if (id !== null && id !== '') {

            var fileExt = '.' + namafile.toLowerCase().split('.').pop();

            if (fileExt == ".pdf") {
                // Display PDF
                var objurl = '@Url.Action("GetFileSurat", "Flow")?id=' + id + '&kantorid=' + kantorid;
                objpdf = objurl;

                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("DocViewerWithoutHeader", "Konten")',
                    success: function (data) {
                        $('#tampilanDokumen').html(data);

                        if (!isexpand) {
                            $("#detailSurat").children(".row").children('div').removeClass()
                            $("#detailSurat").addClass("col-md-4 col-xs-12")
                            $("#detailLampiran").addClass("col-md-8 col-xs-12")
                            $("#detailLampiran").show()
                            isexpand = true
                        }
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                    }
                });
            }
            else if (fileExt == ".jpg" || fileExt == ".png") {
                // Display Image
                var objurl = '@Url.Action("GetFileSuratWithExt", "Flow")?id=' + id + '&kantorid=' + kantorid + '&extension=' + fileExt;
                objfile = objurl;

                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("ImageViewer", "Konten")',
                    success: function (data) {
                        $('#tampilanDokumen').html(data);
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                    }
                });
            }
            else if (fileExt == ".doc" || fileExt == ".docx") {
                // Download Word
                //fileExt = ".doc";
                var params = "id=" + id + '&kantorid=' + kantorid + "&extension=" + fileExt;
                var request = new XMLHttpRequest();
                request.responseType = "blob";
                request.open("GET", "GetFileSuratWithExt" + "?" + params);
                request.onload = function () {
                    var url = window.URL.createObjectURL(this.response);
                    var a = document.createElement("a");
                    document.body.appendChild(a);
                    a.href = url;
                    a.download = this.response.name || namafile
                    a.click();
                }
                request.send();
            }
            else if (fileExt == ".xls" || fileExt == ".xlsx") {
                // Download Excel
                //fileExt = ".xls";
                var params = "id=" + id + '&kantorid=' + kantorid + "&extension=" + fileExt;
                var request = new XMLHttpRequest();
                request.responseType = "blob";
                request.open("GET", "GetFileSuratWithExt" + "?" + params);
                request.onload = function () {
                    var url = window.URL.createObjectURL(this.response);
                    var a = document.createElement("a");
                    document.body.appendChild(a);
                    a.href = url;
                    a.download = this.response.name || namafile
                    a.click();
                }
                request.send();
            }
        }
    }

    var HapusFileLampiran = function (id, namafile) {
        if (id !== null && id !== '') {

            swal({
                title: "Konfirmasi Hapus Data",
                text: "Yakin Anda mau menghapus data lampiran surat " + namafile + " ?",
                type: "warning",
                showCancelButton: true,
                confirmButtonColor: "#DD6B55",
                confirmButtonText: "Ya",
                cancelButtonText: "Batal"
            },
            function (isConfirm) {
                if (isConfirm) {
                    $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
                    var suratid = $('#SuratId').val();
                    var frm = new FormData();
                    frm.append("suratid", suratid);
                    frm.append("id", id);
                    $.ajax({
                        url: '@Url.Action("HapusLampiranSuratById", "Flow")',
                        type: "POST",
                        data: frm,
                        cache: false,
                        contentType: false,
                        processData: false,
                        success: function (data, textStatus, jqXHR) {
                            if (data && data.Status) {
                                $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
                                loadLampiranSurat();
                                showinfo('Data berhasil dihapus');

                                // Tulis ulang jumlah lampiran
                                $.ajax({
                                    url: '@Url.Action("GetJumlahLampiranSurat", "Flow")?suratid=' + suratid + '',
                                    success: function (response) {
                                        $('#LabelJumlahLampiran').html(response);
                                    },
                                    error: function (response) { }
                                });
                            }
                            else {
                                showalert("Error", data.Pesan);
                            }
                            $.unblockUI();
                        },
                        error: function (jqXHR, textStatus, errorThrown) { showmsg("Error", errorThrown); $.unblockUI(); }
                    });
                }
            });
        }
    }

    var dfFileDokumen = null;
    $("#txtUploadFile").on("change", function (e) {
        var input = $(this),
            numFiles = input.get(0).files ? input.get(0).files.length : 1;

        var file = dfFileDokumen = input.get(0).files[0];

        if (numFiles > 0) {

            if (file.size > 20000 * 1024) { dfFileDokumen = null; showmsg('Peringatan', 'File maksimum 20Mb'); return false; }

            // Save to LAMPIRANSURAT
            var suratid = $('#SuratId').val();

            var frm = new FormData();
            frm.append("SuratId", suratid);
            frm.append("namafile", dfFileDokumen.name);
            frm.append("file", dfFileDokumen);

            $.ajax({
                type: "POST",
                url: '@Url.Action("InsertLampiranSurat", "Flow")',
                data: frm,
                contentType: false,
                processData: false,
                success: function (data, textStatus, XMLHttpRequest) {
                    if (data.Status) {
                        $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });

                        loadLampiranSurat();

                        // Tulis ulang jumlah lampiran
                        $.ajax({
                            url: '@Url.Action("GetJumlahLampiranSurat", "Flow")?suratid=' + suratid + '',
                            success: function (response) {
                                $('#LabelJumlahLampiran').html(response);
                            },
                            error: function (response) { }
                        });


                        // Display File -----------------------------------------
                        var tipefile = input.get(0).files[0].type;
                        if (tipefile == 'application/pdf') {

                            // Display PDF --------------------------------------
                            var blob = new Blob([file], { type: "application/pdf;base64" }),
                                objurl = window.URL.createObjectURL(blob);

                            //console.log(blob);
                            objpdf = objurl;
                            var options = { "backdrop": "static", keyboard: true };
                            $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
                            $.ajax({
                                type: "POST",
                                url: '@Url.Action("DocViewerWithoutHeader", "Konten")',
                                success: function (data) {
                                    $('#tampilanDokumen').html(data);
                                    //$.unblockUI();
                                },
                                error: function (XMLHttpRequest, textStatus, errorThrown) {
                                }
                            });
                            // Eof Display PDF ----------------------------------
                        }
                        else if (tipefile == 'image/jpeg' || tipefile == 'image/png') {
                            // Display Image ------------------------------------
                            // Eof Display Image --------------------------------
                        }
                        else {
                            // Download File Word/Excel -------------------------
                            // Eof Download File Word/Excel ---------------------
                        }
                        // Eof Display File -----------------------------------------
                    }
                    else {
                    }
                    $.unblockUI();
                },
                error: function (XMLHttpRequest, textStatus, errorThrown) { $.unblockUI(); }
            });
        }
    });

    var ArsipSurat = function () {
        swal({
            title: "Konfirmasi Arsip Surat",
            text: "Yakin Anda mau mengarsipkan surat tersebut ?",
            type: "input",
            showCancelButton: true,
            closeOnConfirm: false,
            confirmButtonColor: "#DD6B55",
            confirmButtonText: "Ya",
            cancelButtonText: "Batal",
            inputPlaceholder: "Catatan"
        },
        function (inputValue) {
            if (inputValue === false) return false;
            $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
            var suratid = $('#SuratId').val();
            var suratinboxid = $('#SuratInboxId').val();
            var frm = new FormData();
            frm.append("suratid", suratid);
            frm.append("suratinboxid", suratinboxid);
            frm.append("catatananda", inputValue);
            frm.append("Redaksi", redaksi);
            $.ajax({
                url: '@Url.Action("ArsipSurat", "Flow")',
                type: "POST",
                data: frm,
                cache: false,
                contentType: false,
                processData: false,
                success: function (data, textStatus, jqXHR) {
                    if (data && data.Status) {
                        showinfo('Data berhasil diarsip');

                        window.location.href = '@Url.Action("ProsesSurat", "Flow")';
                    }
                    else {
                        showalert(data.Pesan);
                    }
                    $.unblockUI();
                },
                error: function (jqXHR, textStatus, errorThrown) { showmsg("Error", errorThrown); $.unblockUI(); }
            });        });
    }

    $("#BtnTolak").on("click", function (e) {
        swal({
            title: "Konfirmasi Tolak Surat",
            text: "Yakin Anda mau menolak surat ini ?",
            type: "input",
            showCancelButton: true,
            closeOnConfirm: false,
            confirmButtonColor: "#DD6B55",
            confirmButtonText: "Ya",
            cancelButtonText: "Batal",
            inputPlaceholder: "Catatan"
        },
        function (inputValue) {
            if (inputValue === false) return false;
            $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
            var suratid = $('#SuratId').val();
            var suratinboxid = $('#SuratInboxId').val();
            var frm = new FormData();
            frm.append("suratid", suratid);
            frm.append("suratinboxid", suratinboxid);
            frm.append("catatananda", inputValue);
            $.ajax({
                url: '@Url.Action("TolakSurat", "Flow")',
                type: "POST",
                data: frm,
                cache: false,
                contentType: false,
                processData: false,
                success: function (data, textStatus, jqXHR) {
                    if (data && data.Status) {
                        showinfo('Surat berhasil ditolak');

                        window.location.href = '@Url.Action("ProsesSurat", "Flow")';
                    }
                    else {
                        showalert(data.Pesan);
                    }
                    $.unblockUI();
                },
                error: function (jqXHR, textStatus, errorThrown) { showmsg("Error", errorThrown); $.unblockUI(); }
            });
        });
    });

    $("#BtnProses").on("click", function (e) {
        var agenda = $('#NomorAgenda').val();
        var nomor = $('#nomorsurat').val();
        var txt = "";
        if (agenda != "") {
            txt = "No. Agenda : " + agenda + "\nApakah anda mau memproses/menyetujui surat ini ?"
        } else if (nomor != "") {
            txt = "No. Surat : " + nomor + "\nApakah anda mau memproses/menyetujui surat ini ?"
        } else {
            txt = "Apakah anda mau memproses/menyetujui surat ini ?";
        }
        swal({
            title: "Konfirmasi Persetujuan Surat",
            text: txt,
            type: "input",
            showCancelButton: true,
            closeOnConfirm: false,
            confirmButtonColor: "#DD6B55",
            confirmButtonText: "Ya",
            cancelButtonText: "Batal",
            inputPlaceholder: "Catatan"
        },
        function (inputValue) {
            if (inputValue === false) return false;

            $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });

            var frm = new FormData();
            frm.append("SuratId", $('#SuratId').val());
            frm.append("SuratInboxId", $('#SuratInboxId').val());
            frm.append("CatatanAnda", inputValue);

            // Proses SURAT oleh TU ke Tujuan Surat
            $.ajax({
                type: "POST",
                url: '@Url.Action("ProsesSuratMasuk", "Flow")',
                data: frm,
                contentType: false,
                processData: false,
                success: function (data, textStatus, XMLHttpRequest) {
                    if (data.Status) {
                        $.unblockUI();
                        showinfo(data.Pesan);

                        window.location.href = '@Url.Action("ProsesSurat", "Flow")';
                    }
                    else {
                        $.unblockUI();
                        showalert(data.Pesan);
                    }
                },
                error: function (XMLHttpRequest, textStatus, errorThrown) { $.unblockUI(); }
            });
        });

        e.preventDefault();
        return false;
    });

    function loadSuratHistory() {
        $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });
        var id = $('#SuratId').val();
        $.ajax({
            type: 'POST',
            url: '@Url.Action("DaftarSuratHistory", "Flow")',
            data: { suratid: id },
            success: function (data, textStatus, XMLHttpRequest) {

                if (data == 'noresults') {
                    $('#inboxplaceholder').html('');
                }
                else {
                    $('#inboxplaceholder').html(data);
                }
                $.unblockUI();
            },
            error: function (XMLHttpRequest, textStatus, errorThrown) { $.unblockUI(); }
        });
    };

    function JenisSuratChanged() {
        $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });

        var jenissurat = $('#cmbTipeSurat').val();

        if (jenissurat == "Surat Undangan") {
            $("#DivTanggalUndangan").show();
        }
        else {
            $("#DivTanggalUndangan").hide();
            $('#tanggalundangan').val("");
        }

        $.unblockUI();
    };

    $("#frmEditSurat").submit(function (e) {
        if ($("#frmEditSurat").valid()) {

            $.blockUI({ message: '<div style=\"padding:10px\"><b>Sedang proses... </b><p>harap tunggu</p></div>' });

            var frmdata = new FormData(this);

            // Save to SURAT
            $.ajax({
                type: "POST",
                url: '@Url.Action("EditSuratByTU", "Flow")',
                data: frmdata,
                contentType: false,
                processData: false,
                success: function (data, textStatus, XMLHttpRequest) {
                    if (data.Status) {
                        // Success message
                        showinfo(data.Pesan);
                    }
                    else {
                        showalert(data.Pesan);
                    }
                    $.unblockUI();
                },
                error: function (XMLHttpRequest, textStatus, errorThrown) { $.unblockUI(); }
            });
        }

        e.preventDefault();
        return false;
    });

</script>
