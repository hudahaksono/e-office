using Microsoft.AspNet.SignalR;
using System.Collections.Generic;

namespace Surat
{
    public class MyHub : Hub
    {
        public void Send(string kantorid, string message)
        {
            //Models.HomeModel hm = new Models.HomeModel();
            //List<string> satgasFisik = hm.GetSatgasFisik(kantorid);

            //List<string> satgasFisik = new List<string>();
            //Clients.Users(satgasFisik.ToArray()).addNewMessageToPage(message);
        }
    }
}